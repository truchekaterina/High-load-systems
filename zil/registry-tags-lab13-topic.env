# LAB13: топик с ровно 2 партициями (ТЗ). Подключайте вторым --env-file к основному registry-tags-lab8-hl7.env:
#   docker compose --env-file registry-tags-lab8-hl7.env --env-file registry-tags-lab13-topic.env up -d app additional
# Топик создайте на кластере заранее: zil/scripts/kafka_lab13_create_topic_2_partitions.sh
KAFKA_TOPIC=hl07-lab13
