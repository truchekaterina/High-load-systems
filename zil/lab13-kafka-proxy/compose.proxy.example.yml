# Только прокси LAB13 на ВМ нагрузки (2311). Не смешивать с zil/docker-compose.yml на 2307.
services:
  lab13-publish-proxy:
    build: .
    ports:
      - "127.0.0.1:18080:18080"
    environment:
      KAFKA_BOOTSTRAP_SERVERS: "10.60.3.12:9094,10.60.3.13:9094"
      KAFKA_TOPIC: "hl07"
